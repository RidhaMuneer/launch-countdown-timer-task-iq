/**
 * Array of social media icon paths used in the Footer component.
 * 
 * Each string in the array represents the path to a social media icon SVG file.
 * 
 * @type {string[]}
 * 
 * @example
 * ```javascript
 * import { footerIconLinks } from '@/data/icons';
 * 
 * console.log(footerIconLinks);
 * // Output: ["/icon-facebook.svg", "/icon-pinterest.svg", "/icon-instagram.svg"]
 * ```
 */
export const footerIconLinks: string[] = [
  "/icon-facebook.svg",
  "/icon-pinterest.svg",
  "/icon-instagram.svg",
];
